machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t base_ubuntu_16.10 .

dependencies:
  pre:
    - sudo add-apt-repository -y ppa:george-edison55/cmake-3.x
    - ./setup_ubuntu.sh
test:
  pre:
    - mkdir build && pushd build && cmake .. && make && popd
  override:
    - cd build && make test

